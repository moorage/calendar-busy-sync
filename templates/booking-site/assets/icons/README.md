# Public web icons

The public booking page is text-first and does not use SF Symbols. If a user adds icons, vendor a small open-source SVG subset here, keep one stroke weight, and mark decorative icons with `aria-hidden="true"`.

Do not add icon font CDNs, analytics snippets, or third-party runtime scripts to the booking page.
